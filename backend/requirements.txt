fastapi>=0.115.0
uvicorn[standard]>=0.30.0
chromadb>=0.4.0
python-dotenv>=1.0.0
anthropic>=0.30.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
playwright>=1.40.0
duckduckgo-search>=8.0.0
# Optional: enables the cross-encoder reranker (RERANK_METHOD=cross_encoder|auto).
# Reranking falls back to a strong offline heuristic if this is not installed.
# sentence-transformers>=2.2.0
