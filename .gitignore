# ---------- Python ----------
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg-info/
.eggs/
.pytest_cache/
.pytest_tmp/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
build/
dist/

# Virtual environments
.venv/
venv/
env/
ENV/

# ---------- Node / Frontend ----------
node_modules/
dist/
build/
.vite/
.next/
.cache/
coverage/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ---------- Environment files ----------
.env
.env.local
.env.*.local
*.env

# ---------- ChromaDB / local data stores ----------
chroma/
chroma_db/
*.sqlite
*.sqlite3
*.db

# ---------- Query logs (local analytics, never push) ----------
backend/logs/

# ---------- Test-case activity trails (local QA, never push) ----------
backend/test_case_runs/test_case_trail.txt
backend/test_case_runs/test_case_index.txt
backend/test_case_runs/*.log

# ---------- Crawler / pipeline artifacts ----------
crawler/output/
crawler/raw/
crawler/cleaned/
crawler/chunks/
knowledge/cache/

# ---------- Generated crawl intermediates (rebuilt by scripts) ----------
# Pure discovery artifacts regenerated by crawler/scripts. Safe to ignore.
# NOTE: source_registry_merged.csv is intentionally tracked (it carries the
# human PM approval columns), even though a script produces its base rows.
knowledge/catalog_programs.csv
knowledge/discovered_pdfs.csv
knowledge/sitemap_expanded.csv

# ---------- Internal cleanup / execution records (NEVER push) ----------
# Working notes and stabilization/execution logs. Internal only.
docs/cleanup/

# ---------- Editor / OS ----------
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# ---------- Jupyter ----------
.ipynb_checkpoints/

# ---------- Internal PM docs (NEVER push) ----------
# Project-management notes, timelines, reviews, and internal planning live here.
# The rest of docs/ remains public and IS tracked.
docs/pm/

# ---------- Internal development log (NEVER push) ----------
# Human-readable running history for the PM. Internal only.
docs/devlog/

# ---------- Internal Sprint 2 role guides (NEVER push) ----------
# Layman step-by-step guides for Frontend, Backend, and faculty advisors.
# Public handoff stays in docs/sprint2_readiness.md on GitHub.
docs/frontend/
docs/backend/
docs/project-advisor/
docs/design/

# ---------- Internal Cursor config (NEVER push) ----------
# All Cursor rules and local IDE automation stay on each machine only.
# Team-facing rules live in docs/ (e.g. docs/frontend_guidelines.md).
.cursor/

# ---------- Local capture / scratch artifacts (NEVER push) ----------
frontend/tmp/
frontend/artifacts/
frontend/scripts/artifacts/
frontend/docs/pm/
backend/package-lock.json

# ---------- Local QA / capability harness scratch (NEVER push) ----------
tmp/
output/
AskMcNeese_Capability_Report.csv
8.3.26 workable/
docs/CURSOR_HANDOFF_*.md
docs/desktop_empty_header_report.txt

# ---------- Demo credentials with real passwords (NEVER push) ----------
acm/auth/demo-credentials.json
**/demo-credentials.json
!**/demo-credentials.example.json

# ---------- ACM build artifacts ----------
acm/frontend/artifacts/
acm/frontend/node_modules/
acm/backend/.venv/
acm/backend/.pytest_tmp/

# ---------- Regenerated discovery dumps (optional; keep seed registries) ----------
knowledge/domain_discovery_candidates.csv
knowledge/ecosystem_discovered_pages.csv
knowledge/external_site_pages.csv
knowledge/catalog_course_index_102.json

# Full-spectrum research dumps (too large for runtime deploy)
knowledge/full_spectrum/search_queries_50000.jsonl
knowledge/full_spectrum/search_queries_50000.csv

