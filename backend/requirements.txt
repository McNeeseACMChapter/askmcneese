# Deliberately pinned direct runtime dependencies. Update through a reviewed PR,
# run pip check + pip-audit, and execute the complete backend suite.
fastapi==0.137.0
starlette==1.3.1
uvicorn[standard]==0.49.0
chromadb==1.5.9
python-dotenv==1.2.2
anthropic==0.116.0
httpx==0.28.1
beautifulsoup4==4.15.0
duckduckgo-search==8.1.1
playwright==1.61.0
SQLAlchemy==2.0.51
alembic==1.19.1
psycopg[binary]==3.3.4

# Optional: enables the cross-encoder reranker.
# sentence-transformers==5.2.2
